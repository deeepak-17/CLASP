# State Registry — safetensors versioning, Pass@k rollback, FastAPI

**Owner:** Deepak (P4)

Skeleton module. See `.claude/CLAUDE.md` (local) for conventions. Depends on
the shared `contracts` package; keep cross-module interaction interface-driven.

```bash
pip install -e contracts -e services/registry
pytest services/registry/tests
```
