[project]
name = "clasp-cluster"
version = "0.1.0"
description = "CLASP Cluster Layer — Flower + FedProx aggregation, FastAPI (P2)."
requires-python = ">=3.10"
dependencies = [
    "clasp-contracts",
    # "flwr", "fastapi", "uvicorn",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
