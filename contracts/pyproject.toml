[project]
name = "clasp-contracts"
version = "1.0.0"
description = "Shared interface contracts for the CLASP monorepo (integration seam). Frozen v1.0 (Phase II W2)."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
