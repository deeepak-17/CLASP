# experiments

Phase III ablation configs and results. Keep every experiment **config-driven**
(`config.yaml`) so runs are reproducible and diffable. Heavy outputs under
`results/` are gitignored; commit the config and a results summary.
