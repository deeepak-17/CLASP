# CLASP-P5 · Evaluation Harness Dependency Report

_Week 1 · Thursday deliverable — HumanEval/MBPP harness dependencies_  
**Generated:** 2026-08-10T07:07:13Z

## 1. Verdict

**PASS** — environment satisfies every dependency required for Week-1/Week-2 scope

- **Python:** 3.13.13 (minimum 3.11)
- **Platform:** Darwin 25.5.0 (arm64)
- **Required now:** 3
- **Deferred to later weeks:** 5

## 2. Dependency matrix

| Package | Required from | Needed now | Installed | Version | State | Purpose |
| --- | --- | --- | --- | --- | --- | --- |
| PyYAML | Week 1 | yes | yes | 6.0.3 | ok | Parse configs/*.yaml. Every entry point depends on it. |
| pytest | Week 1 | yes | yes | 9.1.1 | ok | Run the unit and integration test suite. |
| jsonschema | Week 2 | yes | yes | 4.26.0 | ok | Structurally validate partition manifests and eval results against the interface contracts (Week-2 Friday sign-off). |
| datasets | Week 3 | no | no | — | not installed (not needed yet) | Fetch the published HumanEval/MBPP task sets from the Hugging Face Hub. The Week-1 dry run uses bundled fixtures and does not need it. |
| human-eval | Week 3 | no | no | — | not installed (not needed yet) | Reference Pass@k implementation, used to cross-check P5's own scorer when it lands in Week 3. |
| torch | Week 3 | no | no | — | not installed (not needed yet) | Tensor runtime for P1's Edge Layer. Needed once the harness calls a real model. |
| transformers | Week 3 | no | no | — | not installed (not needed yet) | Loads DeepSeek-Coder-6.7B. Owned by P1; P5 needs it only to call the merged model. |
| peft | Week 3 | no | no | — | not installed (not needed yet) | LoRA adapter loading for the merged-model eval path. Owned by P1. |

## 3. Deferred dependencies

Absent but not yet needed. These become required when the harness starts calling P1's real merged model and scoring Pass@k (Week 3), so they are declared now to avoid a surprise at integration time.

- `datasets` — Fetch the published HumanEval/MBPP task sets from the Hugging Face Hub. The Week-1 dry run uses bundled fixtures and does not need it. (`pip install datasets`)
- `human-eval` — Reference Pass@k implementation, used to cross-check P5's own scorer when it lands in Week 3. (`pip install human-eval`)
- `torch` — Tensor runtime for P1's Edge Layer. Needed once the harness calls a real model. (`see https://pytorch.org/get-started/locally/ for the correct CUDA build`)
- `transformers` — Loads DeepSeek-Coder-6.7B. Owned by P1; P5 needs it only to call the merged model. (`pip install transformers`)
- `peft` — LoRA adapter loading for the merged-model eval path. Owned by P1. (`pip install peft`)

---

Generated by `evaluation/dependencies.py`. Regenerate with `python scripts/check_dependencies.py`.
